image: node

before_script:
  - npm install

stages:
  - test
  - pages

cache:
  paths:
    - node_modules/

test:
  stage: test
  script:
    - npm run lint

pages:
  stage: pages
  script:
    - npm run generate
  artifacts:
    paths:
      - public
  only:
    - master
